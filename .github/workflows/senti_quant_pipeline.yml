name: Senti-Quant Pipeline

on:
  schedule:
    - cron: '0 1,9 * * 1-5'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: senti-quant-pipeline
  cancel-in-progress: false

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
      PYTHONUNBUFFERED: '1'
      PYTHONPATH: ${{ github.workspace }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate required secrets
        shell: bash
        run: |
          if [ -z "$DATABASE_URL" ]; then
            echo "DATABASE_URL secret is missing"
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Senti-Quant pipeline
        shell: bash
        run: |
          set -o pipefail
          python -m src.main 2>&1 | tee pipeline.log

      - name: Send Telegram notification on success
        if: success()
        shell: bash
        run: |
          CLEANUP_LINE=$(grep '^PIPELINE_CLEANUP_METRICS=' pipeline.log | tail -n 1 || true)
          CLEANUP_JSON="{}"

          if [ -n "$CLEANUP_LINE" ]; then
            CLEANUP_JSON=${CLEANUP_LINE#PIPELINE_CLEANUP_METRICS=}
          fi

          ESCAPED_MESSAGE=$(cat <<EOF | sed ':a;N;$!ba;s/\n/%0A/g'
✅ <b>Senti-Quant Pipeline Sukses</b>

<b>Repository:</b> ${{ github.repository }}
<b>Branch:</b> ${{ github.ref_name }}
<b>Commit:</b> ${{ github.sha }}
<b>Run ID:</b> ${{ github.run_id }}

<b>Cleanup Metrics:</b>
<pre>${CLEANUP_JSON}</pre>

Check logs: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
EOF
          )

          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${ESCAPED_MESSAGE}" \
            -d "parse_mode=HTML" || true

      - name: Send Telegram notification on failure
        if: failure()
        shell: bash
        run: |
          ESCAPED_MESSAGE=$(cat <<'EOF' | sed 's/"/\\"/g' | sed ':a;N;$!ba;s/\n/%0A/g'
          ❌ *Senti-Quant Pipeline Failed*

          *Repository:* ${{ github.repository }}
          *Branch:* ${{ github.ref_name }}
          *Commit:* ${{ github.sha }}
          *Run ID:* ${{ github.run_id }}

          Check logs: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          EOF
          )
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d "chat_id=${TELEGRAM_CHAT_ID}" \
            -d "text=${ESCAPED_MESSAGE}" \
            -d "parse_mode=Markdown" || true